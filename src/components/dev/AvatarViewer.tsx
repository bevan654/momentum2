import React from 'react';
import { View } from 'react-native';

// TODO: Rebuild with ReadyPlayer.Me avatar + RPM animation library + react-three-fiber
export default function AvatarViewer() {
  return <View />;
}
